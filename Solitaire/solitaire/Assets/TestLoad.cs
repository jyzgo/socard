using UnityEngine;
using System.Collections;
using System.IO;
using System;

public class TestLoad : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //var text = Resources.Load<TextAsset>("winablegames/solitaire1");
        //Debug.Log("tt " + text.text[1]);
        ////for(int i = 0; i < ss.Length; i++)
        ////{
        ////    Debug.Log("ss " + ss[i]);
        ////}

        ////int s = Convert.ToInt32(ss[0]);

        //for (int s = 0; s < 52; s++)
        //{
        //    Debug.Log("s -> " + s);

        //    CardColor c = (CardColor)(s / 13);
        //    int num = s % 13;

        //    Debug.Log(" c " + c.ToString() + " card num " + num);
        //}


    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
